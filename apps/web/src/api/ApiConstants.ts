export const protocol = "http";
export const host = window.location.hostname;
export const port = 3000;
export const apiPath = "/api";

export const apiUrl = `${protocol}://${host}:${port}${apiPath}`;
